<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>Users for Basic Auth</h2>
    <a href="/admin/users/new" class="btn btn-success">Add New User</a>
</div>

<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Username</th>
                        <th>Password</th>
                        <th>Created At</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% if @users.empty? %>
                        <tr>
                            <td colspan="4" class="text-center text-muted">No users found. Add one to enable DB-backed authentication.</td>
                        </tr>
                    <% end %>
                    <% @users.each do |user| %>
                        <tr>
                            <td><strong><%= user.username %></strong></td>
                            <td><code><%= user.password %></code></td>
                            <td><%= user.created_at.strftime("%Y-%m-%d %H:%M") %></td>
                            <td class="text-end">
                                <a href="/admin/users/<%= user.id %>/edit" class="btn btn-sm btn-outline-primary">Edit</a>
                                <form action="/admin/users/<%= user.id %>/delete" method="POST" style="display:inline;" onsubmit="return confirm('Are you sure you want to delete this user?');">
                                    <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                                </form>
                            </td>
                        </tr>
                    <% end %>
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="alert alert-info mt-4">
    <h5>How it works:</h5>
    <p>When you create a user, Pechkin automatically generates a secure 12-character password. Use these credentials in your <code>Authorization</code> header as <strong>Basic Auth</strong> when making requests to the API.</p>
    <p>Passwords are stored in the database. If you have <code>.htpasswd</code> file configured, it will still work as a fallback.</p>
</div>
